"""
Page Index Agent Prompts

This agent acts as a structured wrapper around the PageIndex HTTP service
(running at localhost:8000). It does not invoke an LLM directly — the service
handles all AI reasoning internally.

This file is reserved for any future prompt templates that may be needed
for pre/post-processing steps added to the agent.
"""
